# Models Directory

This directory stores:

- **Generated training data** (`sample_training_data.csv`) - Auto-created when needed
- **Trained ML models** (`*.pkl`, `*.joblib`) - Saved after training
- **RL agent models** - Reinforcement learning model checkpoints

**Note**: Files are generated automatically by the system. No manual files required.